services:
  web:
    build: .
    container_name: bni-dach-web
    command: ["sh", "/usr/local/bin/bni-dach-entrypoint"]
    ports:
      - "8082:80"
    volumes:
      - ./app:/var/www/html
      - ./data:/var/www/data
